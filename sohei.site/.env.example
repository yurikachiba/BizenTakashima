# Database connection with recommended timeout parameters for Render.com free tier
# connect_timeout: Time to wait for new connection (30s for cold starts)
# pool_timeout: Time to wait for pool connection (20s)
# connection_limit: Max connections (5 for serverless)
DATABASE_URL="postgresql://user:password@localhost:5432/bizen_takashima?connect_timeout=30&pool_timeout=20&connection_limit=5"
JWT_SECRET="change-this-secret"
DEFAULT_ADMIN_PASSWORD="Qh7A.(LDu%P-"
