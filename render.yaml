# Render.com Blueprint - PostgreSQL Database
# The backend API is now integrated into the Next.js app (deployed on Vercel).
# This blueprint only provisions the PostgreSQL database.
# https://render.com/docs/blueprint-spec

databases:
  - name: bizen-takashima-db
    plan: free
    databaseName: bizen_takashima
    user: bizen_admin
